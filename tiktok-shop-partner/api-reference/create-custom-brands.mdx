---
title: "Create Custom Brands"
sidebarTitle: "Create Custom Brands"
---

## Required scope

seller.product.basic

## Path

`/product/202309/brands`

## Method

`POST`

## Request Header

| Properties | Type | Required | Properties description |
| --- | --- | --- | --- |
| content-type | string | Y | Allowed type: application/json |
| x-tts-access-token | string | Y | The seller access\_token value from [Get Access Token](https://partner.tiktokshop.com/docv2/page/678e3a3292b0f40314a92d75#Get%20Access%20Token%20API), when user\_type = 0. Follow this [guide](https://partner.tiktokshop.com/docv2/page/678e3a344ddec3030b238fa0) to get seller access\_token. |

## Query Header

| Properties | Type | Required | Properties description |
| --- | --- | --- | --- |
| app\_key | string | Y | Every single app will have a unique key. Please use the specific key assigned to your app. |
| sign | string | Y | Signature generated by gen algorithm. When you send API requests to TTS, you must sign them so that TTS can identify the senders. |
| timestamp | int | Y | Unix timestamp GMT (UTC\+00:00). This timestamp is used across all API requests. Developers can use this convert to local time. |

## Request Body

| Properties | Type | Required | Properties description |
| --- | --- | --- | --- |
| name | string | Y | The brand name.<br /><br />**Note**:<br />- Length: \[2, 30\]<br />- No language restrictions, but do not use Chinese.<br />- The name will not be translated into multiple languages. |

## Code Example

### Curl

```shell
curl -X POST \
 'https://open-api.tiktokglobalshop.com/product/202309/brands?app_key=38abcd&sign=5361235029d141222525e303d742f9e38aea052d10896d3197ab9d6233730b8c&timestamp=1623812664' \
-H 'x-tts-access-token: TTP_pwSm2AAAAABmmtFz1xlyKMnwg74T2GJ5s0uQbS8jPjb_GkdFVCxPqzQXSyuyfXdQa0AqyDsea2tYFNVf4XeqgZHFfPyv0Vs659QqyLYfsGzanZ5XZAin3_ZkcIxxS0_In6u6XDeU96k' \
-H 'content-type: application/json' \
-d '{
  "name": "Teas"
}'
```

### Go

```go
// For more details about the SDK, refer to the documentation:
// https://partner.tiktokshop.com/docv2/page/67c83e0799a75104986ae498
func product202309CreateCustomBrandsPost() {
    configuration := apis.NewConfiguration()
    configuration.AddAppInfo(appKey, appSecret)
    apiClient := apis.NewAPIClient(configuration)
    request := apiClient.ProductV202309API.Product202309BrandsPost(context.Background())
    request = request.XTtsAccessToken("your access token")
    request = request.ContentType("application/json")
    product202309CreateCustomBrandsRequestBody := product_v202309.NewProduct202309CreateCustomBrandsRequestBody()
    product202309CreateCustomBrandsRequestBody.SetName("Teas")
    request = request.Product202309CreateCustomBrandsRequestBody(*product202309CreateCustomBrandsRequestBody)
    resp, httpResp, err := request.Execute()
    if err != nil || httpResp.StatusCode != 200 {
        fmt.Printf("productsRequest err:%v resbody:%s", err, httpResp.Body)
        return
    }
    if resp == nil {
        fmt.Printf("response is nil")
        return
    }
    if resp.GetCode()!= 0 {
        fmt.Printf("response business is error, errorCode:%d errorMessage:%s", resp.GetCode(), resp.GetMessage())
        return
    }
    respDataJson, _ := json.MarshalIndent(resp.GetData(), "", "  ")
    fmt.Println("response data:", string(respDataJson))
    return
}
```

### Node.js

```typescript
// For more details about the SDK, refer to the documentation:
// https://partner.tiktokshop.com/docv2/page/67c83e0799a75104986ae498
async function product202309CreateCustomBrandsPost() {
    const product202309CreateCustomBrandsRequestBody = new Product202309CreateCustomBrandsRequestBody();
    product202309CreateCustomBrandsRequestBody.name = "Teas";
    const result = await client.api.ProductV202309Api.BrandsPost("your access token", "application/json", product202309CreateCustomBrandsRequestBody);
    console.log('response: ', JSON.stringify(result, null, 2));
}
```

### Java

```java
// For more details about the SDK, refer to the documentation:
// https://partner.tiktokshop.com/docv2/page/67c83e0799a75104986ae498
public void product202309CreateCustomBrandsPost() throws Exception {
    ApiClient defaultClient = Configuration.getDefaultApiClient()
            .setAppkey("your appKey")
            .setSecret("your secret")
            .setBasePath("https://open-api.tiktokglobalshop.com");
    ProductV202309Api apiInstance = new ProductV202309Api(defaultClient);
    CreateCustomBrandsRequestBody createCustomBrandsRequestBody = new CreateCustomBrandsRequestBody();
    createCustomBrandsRequestBody.setName("Teas");
    CreateCustomBrandsResponse result = apiInstance.product202309BrandsPost("your access token", "application/json", createCustomBrandsRequestBody);
    System.out.println(result);
}
```

## Response Parameters

| Properties | Type | Sample | Properties description |
| --- | --- | --- | --- |
| code | int | 0 | The success or failure status code returned in API response. |
| message | string | Success | The success or failure messages returned in API response. Reasons of failure will be described in the message. |
| request\_id | string | 202203070749000101890810281E8C70B7 | Request log |
| data | object |  | Specific return information |
| ^id | string | 7082427311584347905 | A unique ID that identifies the brand in TikTok Shop. |

## Example

```json
{
  "code": 0,
  "data": {
    "id": "7082427311584347905"
  },
  "message": "Success",
  "request_id": "202203070749000101890810281E8C70B7"
}
```

## Error Code

| Code | Message |
| --- | --- |
| 12052189 | Brand names must be at least 2 characters. |
| 12052190 | Brand names must not be longer than 30 characters. |
| 12052191 | Brand names can't include special fonts or emoticons. |
| 12052192 | The brand name can't contain 5 or more consecutive numbers. |
| 12052202 | The total number of brands you have created today exceeds the limit. |
| 12052203 | The total number of brands you have created exceeds the limit. |
| 12052204 | The brand name you created is invalid. |
| 12052205 | This brand name already exists. |
| 12052206 | The number of characters of the created brand name is invalid. |
| 36009003 | Internal error. Please try again. If the issue persists after multiple attempts, please contact platform support. |